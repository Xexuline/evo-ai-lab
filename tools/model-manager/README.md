# `evo-model` (diseño pendiente)

Herramienta futura para seleccionar perfiles, iniciar o detener `llama-server` y mostrar el estado del modelo. No hay implementación todavía.

Debe operar mediante una interfaz systemd controlada, rechazar perfiles incompletos, evitar cargas concurrentes y no registrar rutas sensibles ni credenciales.
